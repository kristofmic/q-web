import reducerFactory from '../../utils/reducerFactory';

/**
 *
 */
const initialState = {};

const handlers = {};

const configReducer = reducerFactory(initialState, handlers);
export default configReducer;
